# ═══════════════════════════════════════════════════════════════
# Файл: config.py
# Назначение: Настройки проекта (токен бота, ID админов, лимиты).
# Что происходит: Загружает переменные из .env, объявляет
# константы, которые используются в других файлах (auth, main).
# ═══════════════════════════════════════════════════════════════

from dotenv import load_dotenv  # Функция для чтения файла .env
import os                       # Модуль для работы с переменными окружения

load_dotenv()  # Загрузить переменные из файла .env в окружение

# Токен Telegram-бота от @BotFather.
# os.getenv("BOT_TOKEN", "..." ) — взять из переменной окружения BOT_TOKEN,
# а если её нет — использовать запасной токен (второй аргумент).
BOT_TOKEN: str = os.getenv("BOT_TOKEN", "8921084476:AAGiaV28ECeKPXqR_ADiV2MGnZyKRiveIyk")

# Список Telegram ID пользователей, которые могут управлять админ-консолью.
# Если ID нет в этом списке — команды /addmoney и другие не сработают.
ADMIN_IDS: list[int] = [715136637]

# Минимальное количество секунд между двумя пингами от одного пользователя.
# Если прошло меньше — сервер ответит ошибкой 429 (Too Many Requests).
MIN_PING_INTERVAL_S: int = 10

# Максимальная скорость в км/ч, при которой начисляются монеты.
# Если быстрее — считаем, что пользователь в транспорте, монеты не даём.
SPEED_LIMIT_KMH: float = 15.0

# Сколько метров нужно пройти, чтобы получить 1 монету.
# 10 метров = 1 монета.
METERS_PER_COIN: int = 10

# Строка подключения к PostgreSQL.
# os.getenv("DATABASE_URL", "...") — взять из переменной окружения DATABASE_URL,
# а если её нет — использовать локальную БД (walkuser/walkpass на localhost).
# Формат: postgresql://user:password@host:port/database
DATABASE_URL: str = os.getenv("DATABASE_URL", "postgresql://walkuser:walkpass@localhost:5432/walktoearn")
