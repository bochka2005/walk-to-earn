# ═══════════════════════════════════════════════════════════════
# Файл: requirements.txt
# Назначение: Список Python-пакетов, необходимых для проекта.
# Что происходит: Команда pip install -r requirements.txt
# читает этот файл и скачивает/устанавливает все пакеты
# с указанными версиями (>= значит «не ниже этой версии»).
# ═══════════════════════════════════════════════════════════════

fastapi>=0.110.0            # Фреймворк для веб-сервера (API-эндпоинты)
uvicorn[standard]>=0.27.0   # Сервер, который запускает FastAPI (ASGI-сервер)
pydantic>=2.0.0             # Проверка типов данных (классы BaseModel)
geopy>=2.4.0                # Расчёт расстояния между GPS-координатами
python-dotenv>=1.0.0        # Чтение переменных из файла .env
asyncpg>=0.30.0             # Асинхронный драйвер PostgreSQL
